/*function Button(props) {
    let classNames = "";
    if (props.outline) {
        classNames += "";
    } else {
        classNames += "";
    }
    return (
        <button {...props} className={classNames + props.className} />
    );
}

export default Button;*/